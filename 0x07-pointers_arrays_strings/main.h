#ifndef MAIN_H
#define MAIN_H

/**
 * File: holberton.h
 * Descroption: Header file containing prototypes for all functions
 *              used in the 0x07-pointers_arrays_strings directory.
 */

#include <stddef.h>

int _putchar(char c);
void *_memset(void *s, int b, size_t n);
void *_memcpy(void *dest, const void *src, size_t n);

#endif
